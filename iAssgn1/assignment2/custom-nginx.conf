server {
    listen 80;
    server_name localhost;


    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

    #reverse proxy

    location /api/ {
        proxy_pass http://api:5678/; 
    }
}